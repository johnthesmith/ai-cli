[package]
name = "ai"
version = "0.1.0"
edition = "2021"

[dependencies]
core = { path = "../../lib/core" }
ureq = { version = "2.9", features = ["json"] }
serde_json = "1.0"
enigo = "0.2.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
