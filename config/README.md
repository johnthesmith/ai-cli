# Config

1. Files must be placed in the `~/.config/ai/default/` directory:
    1. `config.yaml` - main configuration file;
    2. `prompt.txt` - system prompt template;
    3. `token.txt` - GitHub token file.
